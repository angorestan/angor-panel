import { DatepickerPipe } from './datepicker.pipe';

describe('DatepickerPipe', () => {
  it('create an instance', () => {
    const pipe = new DatepickerPipe();
    expect(pipe).toBeTruthy();
  });
});
